# Verified on the current Kaggle image (Python 3.12).
# kaggle-environments pins gymnasium==1.2.0, which stable-baselines3 <2.7 forbids
# (it caps gymnasium<1.2.0). sb3 2.7+ allows gymnasium<1.3.0, so pin the SB3
# stack to 2.8.0 — sb3-contrib==2.8.0 pulls stable-baselines3 2.8.0.
kaggle-environments>=1.28.0
sb3-contrib==2.8.0
stable-baselines3==2.8.0
tqdm
rich
